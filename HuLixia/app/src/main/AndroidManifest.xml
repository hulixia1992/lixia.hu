<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.drum.hulixia">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".view.MainActivity" />
        <activity android:name=".view.activity.WebActivity" />
        <activity android:name=".view.activity.PictureActivity" />
        <!-- <activity android:name=".daggertest.DraggerTestActivity"> -->


        <!-- </activity> -->
        <!-- <activity android:name=".daggertest.OtherActivit" /> -->
        <!-- <activity android:name=".daggertest.AActivity"></activity> -->
        <activity android:name=".view.AlertActivity" />
        <activity android:name=".view.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.MusicActivity"></activity>
    </application>

</manifest>